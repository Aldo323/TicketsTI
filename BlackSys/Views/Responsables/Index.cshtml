@model IEnumerable<BlackSys.Models.Responsable>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Responsable_name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Responsable_name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id_responsable }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id_responsable }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id_responsable })
        </td>
    </tr>
}

</table>
